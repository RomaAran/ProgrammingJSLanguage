@model List<string>

<h2>Список авторов</h2>

@if (Model.Count == 0)
{
    <p>Авторы не найдены</p>
}
else
{
    <ul>
        @foreach (var a in Model)
        {
            <li>@a</li>
        }
    </ul>
}